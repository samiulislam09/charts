import { ParagraphNode, TextNode } from 'lexical';
import { HeadingNode, QuoteNode } from "@lexical/rich-text"

export const nodes =
  [
    HeadingNode,
    ParagraphNode,
    TextNode,
    QuoteNode,
  ]
